name: Build API

on:
  push:
    branches: [main, develop]
    tags:
      - "v*.*.*"
    paths:
      - "apps/api/**"
      - ".github/workflows/build-api.yml"
  pull_request:
    branches: [main, develop]
    paths:
      - "apps/api/**"
      - ".github/workflows/build-api.yml"
  workflow_dispatch:

jobs:
  api:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./apps/api

    steps:
      - name: ⛓️ Checkout code
        uses: actions/checkout@v7

      - name: 🏗️ Setup Python and uv
        uses: astral-sh/setup-uv@v8
        with:
          python-version: "3.13"
          enable-cache: true
          cache-dependency-glob: apps/api/requirements.txt

      - name: 🏗️ Install API dependencies
        run: |
          uv pip install --system -r requirements.txt
          uv pip install --system pytest httpx flake8

      - name: 🪄 Lint API
        run: flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

      - name: 🎯 Test API
        run: pytest -v tests/

  docker-build-api:
    needs: [api]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    permissions:
      contents: read
      packages: write
      security-events: write
      actions: read

    steps:
      - name: ⛓️ Checkout code
        uses: actions/checkout@v7

      - name: 🏗️ Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: 🪟 Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}-backend
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=ref,event=branch
            type=sha
            latest
          labels: |
            org.opencontainers.image.title=CiBIN-backend
            org.opencontainers.image.description=Application for trash can management
            org.opencontainers.image.version={{version}}
            org.opencontainers.image.created={{date 'YYYY-MM-DDTHH:mm:ssZ'}}

      - name: 🗝️ Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🛠️ API - Build Docker image
        uses: docker/build-push-action@v7
        with:
          context: ./apps/api
          push: false
          load: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          outputs: type=docker,dest=/tmp/api-docker-image.tar
          build-args: |
            BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ')
            VCS_REF=${{ github.sha }}
            VERSION=${{ github.ref_name }}

      - name: 🚛 Push Docker image
        uses: docker/build-push-action@v7
        with:
          context: ./apps/api
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
