POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=citbin
POSTGRES_USER=citbin
POSTGRES_PASSWORD=citbin2025!

ADMIN_PASSWORD=citbin2025!

# --- Logging ---
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# Enable ANSI color codes in console output (true/false)
LOG_COLORS=1
# Use structured JSON format for BOTH console and file (true/false)
# Recommended for production log aggregators (ELK, Grafana, Datadog)
LOG_FORMAT_JSON=1
# Path to the rotating log file (set empty to disable file logging)
APP_LOG_FILE=logs/app.log
# Each HTTP response gets an X-Request-ID header for log correlation
# (configured in app.py, no env var needed)

MQTT_HOSTNAME=citbin.hgs-singen.de
MQTT_PORT=1883
