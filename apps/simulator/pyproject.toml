[project]
name = "citbin-simulator"
version = "0.1.0"
description = "Citbin Simulator"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
